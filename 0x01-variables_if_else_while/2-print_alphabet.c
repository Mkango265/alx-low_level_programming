#include <stdio.h>
/**
 * main - entry program - print alphabet in small letters
 * Return: 0 (success)
 */
int main(void)
{
	int n = 97;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	putchar('\n')
		return (0);
}
